<?xml version="1.0"?>
<block>
  <name>DRM Partitioning</name>
  <key>drm_partitioning_vbvb</key>
  <category>drm</category>
  <import>import drm</import>
  <make>drm.partitioning_vbvb($vlen_in, $vlen_out)</make>
  <!-- Make one 'param' node for every Parameter you want settable from the GUI.
       Sub-nodes:
       * name
       * key (makes the value accessible as $keyname, e.g. in the make node)
       * type -->
  <param>
    <name>Input vector length</name>
    <key>vlen_in</key>
    <type>int</type>
  </param>
  <param>
    <name>Output vector lengths</name>
    <key>vlen_out</key>
    <type>int_vector</type>
  </param>

  <!-- Make one 'sink' node per input. Sub-nodes:
       * name (an identifier for the GUI)
       * type
       * vlen
       * optional (set to 1 for optional inputs) -->
  <sink>
    <name>in</name>
    <type>byte</type>
    <vlen>$vlen_in</vlen>
  </sink>

  <!-- Make one 'source' node per output. Sub-nodes:
       * name (an identifier for the GUI)
       * type
       * vlen
       * optional (set to 1 for optional inputs) -->
  <source>
    <name>out</name>
    <type>byte</type>
    <vlen>$vlen_out</vlen>
    <nports>len(vlen_out)</nports>
  </source>
</block>
