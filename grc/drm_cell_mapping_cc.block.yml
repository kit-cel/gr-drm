# auto-generated by grc.converter

id: cell_mapping_cc
label: DRM Cell mapping
category: '[DRM]'

parameters:
-   id: tp
    label: Transmit Params
    dtype: raw
    hide: part

inputs:
-   label: MSC
    domain: stream
    dtype: complex
-   label: SDC
    domain: stream
    dtype: complex
-   label: FAC
    domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ tp.ofdm().nfft() }

templates:
    imports: import drm
    make: drm.cell_mapping_cc(${tp}, (tp.msc().N_MUX() * tp.ofdm().M_TF() * 8, tp.sdc().N()
        * 8, tp.fac().N() * tp.ofdm().M_TF() * 8))

file_format: 1
