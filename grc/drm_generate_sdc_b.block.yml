# auto-generated by grc.converter

id: drm_generate_sdc_b
label: SDC Generator
category: '[DRM]'

parameters:
-   id: tp
    label: Transmit Parameters
    dtype: raw
    hide: part

outputs:
-   domain: stream
    dtype: byte

templates:
    imports: import drm
    make: drm.generate_sdc_b(${tp})

file_format: 1
