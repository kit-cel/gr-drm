# auto-generated by grc.converter

id: drm_partitioning_4_bb
label: DRM Partitioning 4-QAM EEP
category: '[DRM]'

parameters:
-   id: vlen_in
    label: Input vector length
    dtype: int
    hide: part
-   id: vlen_out
    label: Output vector lengths
    dtype: raw
    hide: part
-   id: vlen1
    label: Length of output
    dtype: int
    hide: part

inputs:
-   domain: stream
    dtype: byte

outputs:
-   label: out1
    domain: stream
    dtype: byte

templates:
    imports: import drm
    make: drm.partitioning_bb(${vlen_in}, ${vlen_out})

file_format: 1
