# auto-generated by grc.converter

id: drm_interleaver_bb
label: Bit Interleaver
category: '[DRM]'

parameters:
-   id: interl_seq
    label: Interleaver Sequence
    dtype: int_vector
    hide: part

inputs:
-   domain: stream
    dtype: byte

outputs:
-   domain: stream
    dtype: byte

templates:
    imports: import drm
    make: drm.interleaver_bb(${interl_seq})

file_format: 1
