# auto-generated by grc.converter

id: drm_interleaver_cc
label: Cell Interleaver
category: '[DRM]'

parameters:
-   id: interl_seq
    label: Interleaving Sequence
    dtype: int_vector
    hide: part
-   id: long_interl
    label: Long Interleaving
    dtype: int
    hide: part
-   id: depth
    label: Interleaving Depth
    dtype: int
    hide: part

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: complex

templates:
    imports: import drm
    make: drm.interleaver_cc(${interl_seq}, ${long_interl}, ${depth})

file_format: 1
