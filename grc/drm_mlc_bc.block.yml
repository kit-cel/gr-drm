# auto-generated by grc.converter

id: mlc_bc
label: MLC Factory Block
category: '[DRM]'

parameters:
-   id: channel_type
    label: Channel Type
    dtype: enum
    default: '""'
    options: ['"FAC"', '"SDC"', '"MSC"']
    option_labels: [FAC, SDC, MSC]
-   id: tp
    label: Transmit Params
    dtype: raw
    hide: part

inputs:
-   domain: stream
    dtype: byte

outputs:
-   domain: stream
    dtype: complex

templates:
    imports: import drm
    make: drm.make_mlc(${channel_type}, ${tp})

file_format: 1
