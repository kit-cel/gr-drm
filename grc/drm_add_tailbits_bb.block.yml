# auto-generated by grc.converter

id: drm_add_tailbits_bb
label: Add Tail Bits
category: '[DRM]'

parameters:
-   id: vlen_in
    label: Input Vector Length
    dtype: int
    hide: part
-   id: n_tailbits
    label: Number of Tail Bits
    dtype: int
    hide: part

inputs:
-   domain: stream
    dtype: byte

outputs:
-   domain: stream
    dtype: byte

templates:
    imports: import drm
    make: drm.add_tailbits_bb(${vlen_in}, ${n_tailbits})

file_format: 1
