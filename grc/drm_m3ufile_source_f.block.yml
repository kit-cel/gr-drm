# auto-generated by grc.converter

id: drm_m3ufile_source_f
label: M3U(8) file source
category: '[DRM]'

parameters:
-   id: filename
    label: file name
    dtype: file_open
-   id: tp
    label: transmit params
    dtype: raw

outputs:
-   domain: stream
    dtype: float

templates:
    imports: import drm
    make: drm.m3ufile_source_f(${filename}, ${tp})

documentation: |-
    Accepts M3U playlist file in standard or extended format and streams its content. There must not be any blank lines in the file. Audio files have to be in WAV format.

file_format: 1
