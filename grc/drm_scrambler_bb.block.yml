# auto-generated by grc.converter

id: drm_scrambler_bb
label: Scrambler
category: '[DRM]'

parameters:
-   id: block_len
    label: Block Length
    dtype: raw
    hide: part

inputs:
-   domain: stream
    dtype: byte

outputs:
-   domain: stream
    dtype: byte

templates:
    imports: import drm
    make: drm.scrambler_bb(${block_len})

file_format: 1
